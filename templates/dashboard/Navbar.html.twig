<nav class="navbar">
  <form class="container-fluid justify-content-start">
    <a href="{{ path('app_home_index') }}" class="btn btn-sm btn-success me-2">Home</a>
    <a href="{{ path('app_home_index') }}" class="btn btn-sm btn-success me-2">Produk</a>
    <a href="{{ path('app_home_index') }}" class="btn btn-sm btn-success me-2">Penjualan</a>
    <a href="{{ path('app_home_index') }}" class="btn btn-sm btn-success me-2">Laporan</a>
    <a href="{{ path('app_staff_index') }}" class="btn btn-sm btn-success me-2">Data Karyawan</a>
    <a href="{{ path('app_roles_index') }}" class="btn btn-sm btn-success me-2">Data Roles</a>
    <a href="{{ path('app_logout_index') }}" class="btn btn-sm btn-secondary">Sign Out</a>
     
    <div class="text-center">
    User : {{ app.session.get('userName') }}
    </div>  
  </form>
  
    
</nav>