{% extends 'base.html.twig' %}

{% block title %}Roles{% endblock %}

{% block body %}
    <h2>Roles</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ role.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_roles_index') }}" class="btn btn-secondary btn-sm">Kembali</a>

    <a href="{{ path('app_roles_edit', {'id': role.id}) }}" class="btn btn-primary btn-sm">edit</a>

    {{ include('roles/_delete_form.html.twig') }}
{% endblock %}
